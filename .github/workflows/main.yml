名字: 刷课
 
在: 
推:
分支: [主要的]
  #是否开始每天8点定时刷课，若要开启请把下面两行注释去掉
  #schedule:
  # - cron: "0 8 * * *"
    
工作: 
开始:
    runs-on: ubuntu-latest # 在最新版本的 Ubuntu 操作系统环境下运行
步骤: # 要执行的步骤
      - 名字: 拷贝代码
用途；使用；利用: actions/checkout@v3  # 用于将github代码仓库的代码拷贝到工作目录中
 
query为空-query为空名字: 设置python环境
使用: actions/setup-python@v2 # 用于设置 Python 环境，它允许你指定要在工作环境中使用的 Python 版本
与:
          python-version: '3.9'  # 选择要用的Python版本
 
      - 名字: 安装依赖包
跑: | # 安装依赖包
pip install -r ./requirements.txt
          
      
      - 名字: 运行main.py
跑: python main. py 19997674193账号 2307801564M coM#课程id，默认为心理课的
